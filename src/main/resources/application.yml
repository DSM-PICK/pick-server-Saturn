spring:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: false
    show-sql: true
  output:
    ansi:
      enabled: always
server:
  address: 0.0.0.0
  port: 8889
  servlet:
    context-path: /saturn